import Btn from "./Btn.js"
import ModeToggler from "./ModeToggler"

const Nav = (props) => <ul><li>{props.first}</li></ul>

function App(props) {
	return (
		<InputComponent />
	);
}

export default App;
